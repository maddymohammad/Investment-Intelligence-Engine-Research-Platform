[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Fast unit tests (no network, no external APIs)
    integration: Integration tests (real DB, no external APIs)
    slow: Tests that require network access or AI API calls
